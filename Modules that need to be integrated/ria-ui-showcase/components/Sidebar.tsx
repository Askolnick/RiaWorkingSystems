import Link from 'next/link';import { centers } from '../app/features/registry';function Pill({s}:{s:'now'|'next'|'planned'}){const map={now:'bg-theme text-white',next:'bg-secondary text-white',planned:'bg-inactive text-text'};const label={now:'Now',next:'Next',planned:'Planned'}[s];return <span className={'text-xs px-2 py-0.5 rounded-full '+map[s]}>{label}</span>}export default function Sidebar(){return (<aside className='w-64 p-4 space-y-4 bg-bg-1 border-r border-border'><Link href='/' className='block text-lg font-semibold mb-2'>Ria — Admin Preview</Link><nav className='space-y-3'>{centers.map(c=>(<div key={c.key}><div className='text-xs uppercase tracking-wide mb-2 text-text-muted'>{c.name}</div><ul className='space-y-1'>{c.features.map(f=>{const href=f.routes?.[0]?.path ?? `/${c.key}/${f.key}`;return <li key={f.key}><Link className='flex items-center justify-between px-2 py-1 rounded-lg hover:bg-bg-2' href={href}><span>{f.name}</span><Pill s={f.status as any}/></Link></li>})}</ul></div>))}</nav></aside>) }