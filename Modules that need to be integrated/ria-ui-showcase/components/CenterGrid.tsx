import Link from 'next/link';import { centers } from '../app/features/registry';export default function CenterGrid(){return (<div className='grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4'>{centers.map(c=>{const first=c.features[0];const href=first?.routes?.[0]?.path ?? `/${c.key}/${first?.key||''}`;return (<Link key={c.key} href={href} className='block rounded-2xl bg-bg-1 border border-border shadow-1 p-5 hover:shadow-2 transition-shadow'><div className='text-sm text-text-muted mb-1'>{c.name}</div><div className='text-lg font-semibold'>{c.features.length} features</div></Link>)})}</div>)}