{
  "name": "@ria/templates",
  "version": "0.1.0",
  "description": "Business planning templates and template engine for RIA",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "jest",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'Linting not configured'"
  },
  "dependencies": {
    "uuid": "^9.0.0",
    "date-fns": "^2.30.0"
  },
  "devDependencies": {
    "typescript": "^5.6.2",
    "@types/node": "^20.0.0",
    "@types/uuid": "^9.0.0",
    "jest": "^29.0.0",
    "@types/jest": "^29.0.0"
  }
}